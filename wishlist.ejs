<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Wishlist - LuxeStore Ultra</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
<header class="navbar">
  <div class="logo">LuxeStore <span>Ultra</span></div>
  <nav>
    <a href="/">Home</a>
    <a href="/products">Shop</a>
    <a href="/cart">Cart</a>
    <a href="/wishlist">Wishlist</a>
  </nav>
</header>

<section class="section">
  <h2>Your Wishlist</h2>

  <% if(wishlist.length > 0) { %>
    <% wishlist.forEach((item, index) => { %>
      <div class="cart-item">
        <h3><%= item.name %></h3>
        <p>₹<%= item.price %></p>
        <a href="/remove-wishlist/<%= index %>" class="btn">Remove</a>
      </div>
    <% }) %>
  <% } else { %>
    <p class="center">Your wishlist is empty.</p>
  <% } %>
</section>
</body>
</html>